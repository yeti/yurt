{
  "name": "yurt",
  "vertion": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --no-cache --parallel --continue",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "eslint": "^8.20.0",
    "turbo": "^1.3.4"
  },
  "packageManager": "yarn@1.22.19"
}
